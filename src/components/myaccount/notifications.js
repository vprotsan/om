import React from 'react';

const Notifications = (props) => (
  <ul>
    <li className="course media group">Notifications</li>
    <li className="course media group">Notifications</li>
    <li className="course media group">Notifications</li>
  </ul>
);

export default Notifications;
