import React from 'react';

const Data = (props) => (
  <ul>
    <li className="course media group">settings</li>
    <li className="course media group">settings</li>
    <li className="course media group">settings</li>
  </ul>
);

export default Data;
